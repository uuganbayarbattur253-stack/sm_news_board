export default function ClubManagerPage() {
  return (
    <div>
      <h2 className="text-2xl font-bold text-gray-900">Club Manager</h2>
      <p className="text-gray-500 mt-1">Join school clubs and record attendance for each session.</p>

      {/*
        TODO: Build your Club Manager app here!

        Suggested form fields:
        - Student name, Club name, Attendance date

        Steps:
        1. Show a list of available clubs
        2. Build a form to join a club
        3. Show a member list below the form
        4. Add a "Mark Present" button for each member
        5. Connect to Supabase to save and load real data

        Helpers you can import:
        - import { supabase } from '@/lib/supabase'
        - import FormField from '@/components/FormField'
        - import SubmitButton from '@/components/SubmitButton'
        - import ItemCard from '@/components/ItemCard'
        - import EmptyState from '@/components/EmptyState'
      */}
    </div>
  )
}
