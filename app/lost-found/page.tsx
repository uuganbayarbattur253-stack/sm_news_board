export default function LostFoundPage() {
  return (
    <div>
      <h2 className="text-2xl font-bold text-gray-900">Lost &amp; Found</h2>
      <p className="text-gray-500 mt-1">Report a lost or found item so classmates can reconnect with their belongings.</p>

      {/*
        TODO: Build your Lost & Found app here!

        Suggested form fields:
        - Item name, Category (Lost / Found), Description, Location, Contact info

        Steps:
        1. Build a form with input fields
        2. Add a submit button
        3. Show a list of reported items below the form
        4. Connect to Supabase to save and load real data

        Helpers you can import:
        - import { supabase } from '@/lib/supabase'
        - import FormField from '@/components/FormField'
        - import SubmitButton from '@/components/SubmitButton'
        - import ItemCard from '@/components/ItemCard'
        - import EmptyState from '@/components/EmptyState'
      */}
    </div>
  )
}
